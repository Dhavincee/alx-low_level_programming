#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 * @argc: Argument count
 * @argv: Argument vetor
 * Return: EXIT_SUCCESS
 */
int main(int argc, char *argv[])
{
	int i,j
	if (argc == 1)
	{
		printf("0\n");
	}
	else if ()
	return (EXIT_SUCCESS);
}
